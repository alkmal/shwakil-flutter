# Use the Play upload keystore here, not the default debug keystore.
# Copy this file to key.properties and fill in the real values locally.
storePassword=your_store_password
keyPassword=your_key_password
keyAlias=your_key_alias
storeFile=../keystore/upload-keystore.jks
