enum AdminTransactionAuditFilter { all, nearBranch, outsideBranches }

enum AdminCustomerSort { balanceDesc, highestDebt, newestFirst, nameAsc }

enum AdminCustomerFilter {
  all,
  verified,
  unverified,
  adminOrSupport,
  highBalance,
  printingDebt,
}
